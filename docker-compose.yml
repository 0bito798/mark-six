services:
  mark-six:
    build: .
    image: ghcr.io/0bito798/mark-six:latest
    container_name: mark-six
    environment:
      - TZ=Asia/Shanghai
      - FLASK_ENV=production
      - PYTHONUNBUFFERED=1
      - SECRET_KEY=${SECRET_KEY:?set SECRET_KEY before starting the service}
      - ENABLE_SCHEDULER=1
      # MySQL support (optional)
      # - DB_TYPE=mysql
      # - DB_HOST=127.0.0.1
      # - DB_PORT=3306
      # - DB_NAME=mark_six
      # - DB_USER=root
      # - DB_PASSWORD=your_password
      # or use a full DATABASE_URL, e.g. mysql+pymysql://user:pass@host:3306/mark_six?charset=utf8mb4
    volumes: 
      - ./data:/app/data
    ports:
      - "5000:5000"
    restart: unless-stopped
